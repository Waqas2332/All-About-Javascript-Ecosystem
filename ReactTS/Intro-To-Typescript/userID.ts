// * Idea behind this program is to get the user ID that can either be string or number

let userID: number | string; // ? defining union types that it can be either string or number

userID = "abd123";

userID = 123;
